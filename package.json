{
  "name": "bit-rate-maximizer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A browser game that maximizes a human player's achieved bit rate: click the highlighted cell of a calibrated grid (v2). The original falling-lanes keyboard version is kept as v1.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && npm run build:v1",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build:v1": "BRM_VARIANT=v1 vite build --outDir dist/v1 --emptyOutDir",
    "dev:v1": "BRM_VARIANT=v1 vite"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
